" Needed for the lightline status bar
set laststatus=2

" Remove the -- INSERT -- message as lightline takes care of it
set noshowmode

" Enable syntax highlighting
syntax enable

" Use the intellij Darcula color palatte
colorscheme darcula
